apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: frontend-service
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/NASRHOUDA/frontend-service
  ref:
    branch: main
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: frontend-service
  namespace: flux-system
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: frontend-service
  path: ./kubernetes
  prune: true
  targetNamespace: taskmanager
  wait: true
  timeout: 3m
