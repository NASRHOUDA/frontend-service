apiVersion: v1
kind: Service
metadata:
  name: frontend-microservice
  namespace: taskmanager
spec:
  selector:
    app: frontend-microservice
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
